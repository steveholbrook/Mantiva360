{
  "name": "mantiva360-marketing",
  "version": "1.0.0",
  "private": true,
  "description": "Mantiva360 public marketing website",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-site.mjs",
    "preview": "python3 -m http.server 4173 --directory public"
  },
  "engines": {
    "node": ">=22"
  }
}
